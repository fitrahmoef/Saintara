# Server Configuration
PORT=5000
NODE_ENV=development

# ============================================
# Database Configuration
# ============================================
# OPTION 1: Use Neon (Serverless PostgreSQL) - Recommended for Production
# Uncomment the line below and add your Neon connection string
# DATABASE_URL=postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require

# OPTION 2: Use Local PostgreSQL - For Development
# If DATABASE_URL is not set, these will be used:
DB_HOST=localhost
DB_PORT=5432
DB_NAME=saintara
DB_USER=postgres
DB_PASSWORD=your_password

# ============================================
# JWT Configuration
# ============================================
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d

# ============================================
# CORS Configuration
# ============================================
CORS_ORIGIN=http://localhost:3000

# ============================================
# Notes:
# ============================================
# - For Neon setup, see NEON_SETUP.md
# - DATABASE_URL takes precedence over individual DB_* variables
# - Never commit .env file to git
# - Use strong JWT_SECRET in production
# - Update CORS_ORIGIN to your frontend URL in production
